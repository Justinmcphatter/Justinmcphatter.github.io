function fetchGradeData() {
    console.log("fetchGradeData function called");
    // This function will later fetch data from PostgreSQL
}

function populateGradebook(data) {
    console.log("populateGradebook function called with data:", data);
    // This function will later populate the table with the fetched data
}